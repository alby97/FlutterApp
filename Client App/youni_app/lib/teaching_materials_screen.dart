import 'package:flutter/material.dart';

class TeachingMaterialsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Text("Materiale didattico",
      style: TextStyle(
        color: Colors.white,
        fontFamily: 'Avenir Next',
        fontSize: 20
      ),),
    );
  }

}